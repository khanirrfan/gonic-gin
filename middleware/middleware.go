package middleware

import "github.com/gin-gonic/gin"

func Authentication(c *gin.Context) {
	// logic
	// get the request header token,
	// validate the resource type to access
	// find the user id,
	// create global payload type and assign
	// return
}
